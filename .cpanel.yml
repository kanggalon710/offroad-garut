---
deployment:
  tasks:
    # Setiap baris di sini dijalankan sebagai shell terpisah, jadi `source`
    # tidak ikut ke baris berikutnya. Karena itu seluruh langkahnya ditaruh
    # di satu skrip yang menurunkan jalurnya sendiri, supaya sama benarnya
    # untuk aplikasi produksi maupun dev.
    - bash .cpanel/deploy.sh
