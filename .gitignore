node_modules
.next
out
build
.DS_Store
*.pem

.env
.env.local
.env.production
.env*.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

next-env.d.ts
.vercel
tsconfig.tsbuildinfo

# Pengaturan Claude Code yang khusus mesin ini
.claude/settings.local.json

drizzle/meta

# Berkas yang diunggah pengelola saat aplikasi berjalan. Tidak boleh ikut ter-commit.
public/uploads/

# Berkas kerja fitur pembaruan (status, log, kunci, penanda restart).
tmp/
# Cadangan hasil build sebelumnya, dipakai pemulihan otomatis. Tanpa baris ini
# ia membuat working tree terlihat kotor dan pembaruan berikutnya selalu ditolak.
.next-sebelumnya/
restart.txt

# Konfigurasi Apache milik server, bukan milik repo. cPanel yang menulisnya dan
# isinya berbeda per lingkungan (PassengerAppRoot menunjuk direktori yang berbeda
# di produksi dan staging), jadi tidak boleh ikut ter-commit. Tanpa baris ini
# berkasnya terhitung untracked, working tree selalu kotor, dan `perbarui.cjs`
# menolak jalan sehingga tombol /pembaruan mustahil dipakai.
.htaccess
